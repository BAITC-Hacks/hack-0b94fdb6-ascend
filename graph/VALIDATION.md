# Проверка поставки — 23.09.2026

- 49 автономных тестов прошли (pytest, Python 3.11.8): синтетические правила, coordinator и его структурные условия, валидация, точность идентификаторов, повторяемость, операции агента, управление вызовами/историей/таймаутом, отклонение неподтверждённых GID и числовых токенов, ошибки HTTP-адаптера.
- Два полных запуска предоставленного датасета: одинаковые nodes/edges/clusters/top и побайтово одинаковые три CSV. Время первого из пары — 9.496219 с с раскладкой.
- Узлы 2248, рёбра 3119, транзакции 4840, seed 81, кластеры 91, компоненты 35, изоляты 19, граница 444. Предупреждений 0. Сверка сумм и количества транзакций пройдена.
- Все идентификаторы сохранены без дублей и потери точности; JSON-идентификаторы строковые; scores конечны и в [0,1]; top-20 совпадает с nodes_roles; кластеры покрывают все узлы; evidence с числами, длина максимум 168 символов; граничных terminal нет.
- Синтаксическая проверка всех Python-модулей пройдена.

Запуск тестов: `python -m pytest graph/tests -q`. FastAPI-тест пропускается, если необязательный backend-стек не установлен; остальные тесты работают на graph/requirements-dev.txt. Тест агента использует подставной Responses-клиент и не требует SDK/ключа/сети.

Результаты реального датасета хранятся локально, в Git опубликован исходный код и сводные показатели. Живой OpenAI-вызов, общий pipeline/backend, frontend, Telegram и приёмка всего продукта этой проверкой не подтверждаются.
