# Проверка поставки 23.09.2026

Проверено после включения обновлений main 52a684a (frontend) и 9f6ad1d (backend).

- `python -m pytest graph/tests ai_agent/tests backend/tests -q`: 86 passed, 1 skipped, 21 subtests passed. Пропущен отдельный opt-in тест backend на локальном датасете; реальные данные проверены отдельным запуском ниже.
- Полный pipeline на трёх parquet из пользовательской папки data: 2248 узлов, 3119 рёбер, 4840 транзакций. CSV прошли проверки. Распределение ролей совпало с предыдущим прогоном пользователя.
- С реальным опубликованным снимком: health и status агента показывают один run_id, Top-1 совпадает с карточкой агента, /assistant доступна. При отключённом OpenAI API графа продолжает работать.
- В браузере проверены отключённый агент и реальные карточки. Отдельный тестовый сервер с явно помеченным подставным ответом проверил отправку, вывод ответа, ссылку GID и открытие реальной карточки. Подставной сервер не входит в поставку и не является проверкой OpenAI.
- Автотесты включают реальный цикл инструментов с подставным клиентом Responses, сбои, таймаут, лимиты, grounding и совместимость с новым backend.

На момент первоначальной поставки живой запрос OpenAI не проверялся; результаты последующей проверки приведены ниже. Интеграцию виджета в основной frontend завершает его владелец по README; это отдельная совместная приёмка.


## Исправление отклонённых ответов

После настройки локального ключа проверены живые запросы на gpt-4.1-mini. Причина частых отклонений: округление дробей и эквивалентная запись p99 словами. Теперь процентиль распознаётся в обоих форматах, а неподтверждённый черновик возвращается модели на исправление внутри прежних лимитов пяти шагов и тридцати секунд. Неуспешный черновик пользователю не показывается; выдуманные GID и числа по-прежнему отклоняются.

Регрессия: 88 passed, 1 skipped, 21 subtests passed. Добавлены проверки процентиля и исправления округлённого черновика. Живой HTTP 200 получен для обоих узлов со скриншота пользователя (100000008477350100 и 100000008603629100), вопроса о приоритетах и вопроса об общих получателях пяти seed. Проверены фактические вызовы get_node, get_top и common_downstream. Это подтверждает работоспособность проверенных сценариев, но не гарантирует безошибочность любых будущих ответов модели. Семантические выводы необходимо сверять с карточками и ограничениями инструментов.
